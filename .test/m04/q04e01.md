*  Can you help me with the subsequent "I don’t want to set the world on fire" project at 42 School?

---

- Files to turn in: `Makefile`, `main.cpp`, `*.hpp`, `*.cpp`
- Forbidden functions: None

---

Constructors and destructors of each class must display specific messages.

Implement a **Brain** class. It contains an array of 100 std::string called `ideas`.  \
This way Dog and Cat will have a private Brain* attribute.  \
Upon construction, Dog and Cat will create their Brain using `new Brain();`  \
Upon destruction, Dog and Cat will delete their Brain using `delete`.  \

In the main function, create and fill and array of **Animal** objects.  \
Half of it will be **Dog** objects, the other half **Cat** objects.  \
At the end of your program execution, loop over this aray and delete every Animal.  \
You must delete directly dogs and cats as Animals (i.e., using a base class pointer). The appropriate destructors must be called in the expected order.

Don't forget to check for memory leaks:
```cpp
int main()
{
    const Animal* dog = new Dog();
    const Animal* cat = new Cat();

    delete dog; // should not create a memory leak
    delete cat; // should not create a memory leak
    ...
    return 0;
}
```

A copy of a dog or a Cat mustn't be shallow. You have to test that your copies are deep copies!

---

Implement your own comprehensive tests to ensure that all of your code works as expected.

---

*  Please put comments and give me explanations of what and why is done.

*  I don't have experience with C++, but I know C and I know object-oriented programming concepts. Please explain me how to implement this in C++ and everything what is new to me compared to C.
