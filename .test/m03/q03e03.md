*  Can you help me with the subsequent "Now it’s weird!" project at 42 School?

---

- Files to turn in: `Makefile`, `main.cpp`, `ClapTrap.hpp`, `ClapTrap.cpp`,  `ScavTrap.hpp`, `ScavTrap.cpp`, `FragTrap.hpp`, `FragTrap.cpp`, `DiamondTrap.hpp`, `DiamondTrap.cpp`
- Forbidden functions: None

---

Now implement a **DiamondTrap** class that inherits from both ScavTrap and FragTrap. This class will combine the features of both parent classes.

The DiamondTrap class will have a private attribute named *name*. this attribute must have
exactly the same variable name as in the ClapTrap base class (without referring to the robot's name).

To be clearer, here are two examples:
  - if ClapTrap's variable is *name*, give the diamondTrap's variable the name *name*.
  - if ClapTrap's variable is *_name*, give the diamondTrap's variable the name *_name*.

Its attributes and member functions will be inherited from its parent classes:
  - Name, which is passed as a parameter to the constructor
  - ClapTrap::name (parameter of the constructor + '_clap_name' suffix)
  - Hit points (inherited from FragTrap, 100)
  - Energy points (inherited from ScavTrap, 50)
  - Attack damage (inherited from FragTrap, 30)
  - attack() (inherited from ScavTrap)

In addition to the special functions from both parent classes, DiamondTrap will have its own unique member function:
  - `void whoAmI(void);`  \
  This member function will display both its name (the DiamondTrap's name) and its ClapTrap::name.

Of course, the ClapTrap instance of DiamondTrap will be created once, and only once.
Yes, there is a way to do it. Find it.

(q): Do you know the -Wshadow and -Wno-shadow compiler flags?

---

Implement and add even more tests to ensure the proper functionality.

---

*  Please put comments and give me explanations of what and why is done.

*  I don't have experience with C++, but I know C and I know object-oriented programming concepts. Please explain me how to implement this in C++ and everything what is new to me compared to C.
