Help me please with the next project: **Sed is for losers**

- Files to turn in: `Makefile`, `main.cpp`, `*.hpp`, `*.cpp`
- Forbidden functions: `std::string::replace`

- Create a program that takes three parameters in the following order: a filename and two strings: `s1` and `s2`.
- It must open the file \<filename> and copy its content into a new file \<filename>.replace, replacing every occurence of `s1` with `s2`.
- Using C file manipulation functions is forbidden and will be considered cheating.
- All the member functions of the class `std::string` are allowed, except `replace`
- Handle unexpected inputs and errors.
- Create and turn in your tests to ensure that the program works as expected.
